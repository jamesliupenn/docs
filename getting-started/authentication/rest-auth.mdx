---
title: REST API Authentication
description: 'Getting Access to the DIMO APIs'
---

<Warning>
    Developers need to be registered with DIMO by providing your preferred `client_id` and `redirect_uri`. Please reach out to us on Discord for this.
</Warning>

1. Registered `client_id` and `redirect_uri`
2. An Ethereum address for your wallet (a 64 character hex string generated subject to various rules defined in the [Ethereum Yellow Paper](https://ethereum.github.io/yellowpaper/paper.pdf))
3. The `private_key` of your wallet. To obtain the `private_key` of your wallet, please follow the Developer FAQ.

<Info>
    The `private_key` allows you to validate your sign in to your wallet `0xA`, so you'll get to see what `0xA` has access to on the blockchain. 
</Info>

### Step 1: Generate Challenge

You will need to first generate the Web3 challenge by sending a `POST` request to DIMO's authentication endpoint. From here, you will be able to generate a `state` for Step 3 and a `challenge` for Step 2.

<CodeGroup>

    ```curl cURL
    curl --location --request POST 'https://auth.dev.dimo.zone/auth/web3/generate_challenge?client_id=string&domain=string&scope=string&response_type=string&address=string' \
    --header 'Accept: application/json' \
    --header 'Content-Type: application/json'
    ```

    ```javascript NodeJS
    var request = require('request');
    var options = {
        'method': 'POST',
        'url': 'https://auth.dev.dimo.zone/auth/web3/generate_challenge?client_id=string&domain=string&scope=string&response_type=string&address=string',
        'headers': {
            'Accept': 'application/json',
            'Content-Type': 'application/json'
        }
    };
    request(options, function (error, response) {
        if (error) throw new Error(error);
        console.log(response.body);
    });
    ```

    ```python Python
    import requests
    import json

    url = "https://auth.dev.dimo.zone/auth/web3/generate_challenge?client_id=string&domain=string&scope=string&response_type=string&address=string"

    payload = {}
    headers = {
        'Accept': 'application/json',
        'Content-Type': 'application/json'
    }

    response = requests.request("POST", url, headers=headers, data=payload)

    print(response.text)
    ```
</CodeGroup>

```json Sample Responses
SignedMessage(messageHash=HexBytes('0x629749f84ee0d301f8eb02f3ef897b0c0a4553d2fdxd1fa0ea5cbbb6ff8bdfd5'), r=48064598734910045938712268846365002327886250846331651560012768739017714013389, s=54468135635475555126928311975236101130284121664770021136367631881584318718503, v=27, signature=HexBytes('0x6a43994c94195bf873b1025ae5778768a6311fb846bc21f5aeffc71f058394cd786bdf4d27e43e9a2358a4a0018f1af0333cbbf6318d6dd2be929e5f3fc9a2271b'))
```

### Step 2: Sign Challenge

### Step 3: Submit Challenge